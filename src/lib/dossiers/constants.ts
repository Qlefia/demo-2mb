/** Freshness window for Section 3 signals (aligned with quality checklist UX copy). */
export const DOSSIER_SIGNAL_MAX_AGE_DAYS = 180

/** Section 7: each hook must be at least this long. */
export const DOSSIER_HOOK_MIN_CHARS = 20

/** Section 7: exact number of hooks required. */
export const DOSSIER_HOOK_REQUIRED_COUNT = 3
