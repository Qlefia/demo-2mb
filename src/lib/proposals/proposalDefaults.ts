/** Default validity window for new proposals (business days semantics are copy-only). */
export const PROPOSAL_DEFAULT_VALIDITY_DAYS = 3

/** When preset includes an empty `service_tags` block, pre-fill from the library. */
export const SERVICE_TAGS_AUTOFILL_COUNT = 8
